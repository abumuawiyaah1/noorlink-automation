[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "uvicorn src.api.main:app --host 0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 180

[environment]
PYTHON_VERSION = "3.9"
